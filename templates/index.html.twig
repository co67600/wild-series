{% extends 'base.html.twig' %}


{% block seo_description %}description SEO{% endblock %}

{% block body %}

    <h1>Bienvenue sur {{ website }} !</h1>

    <a href="{{ path('wild_show', { 'slug': "walking-dead" }) }}">
        Testing show() method from WildController with a real program s slug.
    </a>
    <br>
    <a href="{{ path('wild_index') }}">
        Testing index() method from WildController to view all programs.
    </a>
    <br>
    <a href="{{ path('show_category', { 'categoryName': "Horreur" }) }}">
        Testing show() method from WildController to view programs by category "Horreur".
    </a>
    <br>
    <a href="{{ path('show_category', { 'categoryName': "Fantastique" }) }}">
        Testing show() method from WildController to view programs by category "Fantastique".
    </a>
    <br>
    <a href="{{ path('wild_episode', {'id' : 30 }) }}">
        Testing show() method from WildController to view episodes.
    </a>

    <br>
    <a href="{{ path('show_actor', {'id' : 1 }) }}">
        Testing show() method from WildController to view actors.
    </a>

    <br>
    <a href="{{ path('add_category') }}">
        Testing add category
    </a>


{% endblock %}
